import Typography from "@/components/base/Typography";

export default function EmptyTitle() {
  return (
    <Typography variant={{ base: "title-3", lg: "title-2" }} >
      Awaiting New Schedule
    </Typography>
  )
}